type InitialCurrentUserState = {
  currentUser: null;
};

type InitialDarkThemeState = {
  isDarkTheme: boolean;
};

type InitialLocationState = {
  location: string;
};
type AirQuality = {
  text: string;
  color: string;
  categoryNum: number;
};
